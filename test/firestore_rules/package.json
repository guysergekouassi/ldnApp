{
  "name": "jep-firestore-rules-test",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "firebase emulators:exec --only firestore --project jep-rules-test \"node regles_test.mjs ../../firestore.rules\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.2",
    "firebase": "^12.0.0",
    "firebase-tools": "^13.35.1"
  }
}
